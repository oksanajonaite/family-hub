package com.familyhub.config;

// Remember-me is configured directly in SecurityConfig.
// This file is kept as a placeholder for future token persistence (e.g. DB-based PersistentTokenRepository).
// For v1, cookie-based (hash) remember-me is sufficient — no extra beans needed here.
